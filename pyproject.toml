[project]
name = "pg-db-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=45.0.7",
    "psycopg[binary,pool]>=3.2.9",

]

[project.non-win]
dependencies = [
    "pyminizip>=0.2.6",
]